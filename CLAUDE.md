# CLAUDE.md — mission-timer

此檔案只管 `mission-timer` 這個資料夾。以下規則每次進來處理這個專案都要遵守，除非 Vais 明確說要改。

1. 深色星空背景，主色只用在當下要強調的那一個元素。
2. 星球與火箭一律用 canvas 或 CSS 畫，不得引用外部圖片。
3. 按鈕文案使用航太語彙：發射、待機、返航、補給。
4. 倒數的分鐘數要放在最上面當設定，不要散在程式碼裡。

其他既定的樣式做法（色彩變數系統、字體分工、狀態切換模式等）見同資料夾的 [DESIGN.md](DESIGN.md)。
